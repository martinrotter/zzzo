<UserControl x:Class="ZZZO.Controls.Generator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ZZZO.Controls"
             xmlns:converters="clr-namespace:ZZZO.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <converters:InverseBooleanConverter x:Key="InverseBooleanConverter" />
  </UserControl.Resources>
  <WrapPanel>
    <Button IsEnabled="{Binding App.GeneratorDocx.IsGenerating, Converter={StaticResource InverseBooleanConverter}}"
            Click="GenerateDocx">
      Vygenerovat DOCX!
    </Button>
    <Button IsEnabled="{Binding App.GeneratorHtml.IsGenerating, Converter={StaticResource InverseBooleanConverter}}"
            Click="GenerateHtml">
      Vygenerovat HTML!
    </Button>
    <ProgressBar Name="PbGenerator" MinWidth="150" Minimum="0" Maximum="100" />
  </WrapPanel>
</UserControl>